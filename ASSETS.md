# 素材与字体说明

## 内置素材

`assets/style-packs/` 下的画风参考图和 `assets/bubble-packs/` 下的气泡图均由项目作者使用 AI 生成，并由作者确认可以随本仓库公开发布。每个素材包的 `配置.json` 都记录了相同的来源声明。

这些作者提供的素材与软件和配套 skill 一并采用仓库根目录的 MIT License。

这些图片是工作台的示例/内置素材，不代表任何现实品牌、商业动画作品或现有角色。使用者仍需对自己生成的内容、输入参考图和最终作品承担版权与平台合规责任。

## 字体

项目不捆绑字体文件。网页界面使用 CSS 字体栈；导出文字会优先尝试 Windows 系统字体（例如 `C:/Windows/Fonts/NotoSansSC-VF.ttf`），找不到时回退到 Pillow 默认字体。不同操作系统的字形和换行可能略有差异。

## 示例项目

公开仓库只应包含作者确认可以再分发的脱敏项目数据和图片。计划中的“星落纪元”示例需要以项目导出 JSON、镜头图片和明确的再分发授权提交；浏览器截图不等同于可导入示例数据。

如果把工作台截图放入 README 或 Release，请先裁掉浏览器标签栏、桌面窗口、会议/远程协作浮层和用户名，再检查图片边角是否有项目路径、账号或通知内容。
